export function PromptLab() {
  return (
    <section className="space-y-6">
      <div>
        <h1 className="text-2xl font-bold text-ink">Prompt Lab</h1>
        <p className="mt-2 text-sm text-muted">Interactive mock extraction experiments arrive in Pass 2.</p>
      </div>
      <div className="rounded-lg border border-line bg-white p-8 text-sm text-slate-600">
        The default pipeline uses mock extraction so the project is free to run locally.
      </div>
    </section>
  );
}

